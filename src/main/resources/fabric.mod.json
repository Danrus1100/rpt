{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${author}"
  ],
  "license": "MIT",
  "icon": "assets/rpt/icon.png",
  "accessWidener": "rpt.accesswidener",
  "environment": "client",
  "entrypoints": {
    "client": [
      "com.danrus.rpt.Rpt"
    ]
  },
  "mixins": [
    "rpt.mixins.json",
    "rpt.12111.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.17",
    "minecraft": "${mcdep}",
    "rpf": ">=1.2.2"
  }
}